SUPABASE_URL=###
SUPABASE_KEY=###
# Mirror these into .dev.vars for Cloudflare workerd local dev (`npm run dev`)
OPENROUTER_API_KEY=###
# Local dev only: skip OpenRouter and return a fixed plan (never set on production Worker)
# PLAN_GENERATION_MOCK=true