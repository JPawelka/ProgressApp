SUPABASE_URL=###
SUPABASE_KEY=###
# Mirror these into .dev.vars for Cloudflare workerd local dev (`npm run dev`)
GEMINI_API_KEY=###
# Local dev only: skip Gemini and return a fixed plan (never set on production Worker)
# PLAN_GENERATION_MOCK=true
# Playwright E2E (gitignored .env or the environment — never commit real values)
# E2E_EMAIL=
# E2E_PASSWORD=